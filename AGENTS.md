修改完powershell相关文件后，执行pnpm qa:pwsh修复出现的问题

当创建或更新 `openspec/changes/**` 与 `openspec/specs/**` 下的 OpenSpec 工件（`proposal.md`、`design.md`、`tasks.md`、`specs/**/*.md`、`validation.md`）时，除 OpenSpec 结构关键词与固定术语（`ADDED Requirements`、`MODIFIED Requirements`、`REMOVED Requirements`、`RENAMED Requirements`、`Requirement`、`Scenario`、`WHEN`、`THEN`、`BREAKING`）外，其余叙述必须使用简体中文；代码、命令、路径、参数名保持原样。
