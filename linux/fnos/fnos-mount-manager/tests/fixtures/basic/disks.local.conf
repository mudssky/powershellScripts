mount_root "/mnt/local"
default_fs "ntfs"
default_mode "automount"
default_options "nofail,uid=1000,gid=1000,umask=022"
default_device_timeout "45"

disk "bookDisk" "LABEL:local-book" mount="books/bookDisk" options="windows_names"
disk "debutDisk" "UUID:local-debut" mode=eager options="big_writes"
