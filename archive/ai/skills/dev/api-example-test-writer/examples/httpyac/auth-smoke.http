###
# 说明：验证测试账号能完成登录，并为后续受保护接口提供访问令牌。
# 前置：baseUrl/username/tenantId 来自 .env.test，password 来自 CI secret 或 .env.local。
# 断言：只确认 token 存在，避免把 token 格式或签名实现绑定进示例。
# @name login
# @tag test
# @tag smoke
POST {{baseUrl}}/api/auth/login
Content-Type: application/json

{
  "username": "{{username}}",
  "password": "{{password}}",
  "tenantId": "{{tenantId}}"
}

?? status == 200
?? body $.data.token exists

{{response
  const body = JSON.parse(response.body);
  exports.authToken = body.data.token;
}}

###
# 说明：读取当前用户信息，确认 login 产生的令牌能访问基础受保护接口。
# 前置：依赖 login 请求成功；失败时优先检查认证中间件、测试账号和 token 提取路径。
# 断言：只检查稳定身份字段，避免把展示型用户资料写成冒烟测试硬约束。
# @name me
# @tag test
# @tag smoke
# @ref login
GET {{baseUrl}}/api/me
Authorization: Bearer {{authToken}}

?? status == 200
?? body $.data.id exists

###
# 说明：本地排查登录失败时手动发送，不进入自动化测试或 CI。
# 风险：debug@example.com 只是占位账号；真实账号和密码必须来自本机私有 env。
# @name login-manual
# @tag manual
POST {{baseUrl}}/api/auth/login
Content-Type: application/json

{
  "username": "{{debugUsername}}",
  "password": "{{password}}",
  "tenantId": "{{tenantId}}"
}
