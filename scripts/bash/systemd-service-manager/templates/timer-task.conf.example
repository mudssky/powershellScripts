DESCRIPTION=Cleanup Temp Files
TARGET_TYPE=task
COMMAND=/usr/bin/find /tmp/myapp -type f -mtime +7 -delete
WORKDIR=/opt/myapp
USER=myapp
GROUP=myapp
SCHEDULE=0 3 * * *
PERSISTENT=true
RETRY_ATTEMPTS=1
RETRY_DELAY_SEC=5
