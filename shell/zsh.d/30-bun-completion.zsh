# ======================================================================
# 文件：30-bun-completion.zsh
# 作用：在交互式 Zsh 中加载 Bun completion。
# 兼容性：Zsh。
# ======================================================================

# -- completion ---------------------------------------------------------
if [ -s "$HOME/.bun/_bun" ]; then
    source "$HOME/.bun/_bun"
fi
