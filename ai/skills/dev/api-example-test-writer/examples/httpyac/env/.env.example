# 可提交模板：只声明变量名、非敏感默认值和占位说明。
baseUrl=http://localhost:3000
username=api-test@example.com
debugUsername=debug@example.com
tenantId=demo
requestTimeout=30000

# 真实密码和固定 token 不提交，使用 .env.local 或 CI secret 注入。
password=<from-local-or-ci-secret>
token=<only-for-manual-or-non-login-auth>
